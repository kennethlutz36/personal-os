(()=>{'use strict';if(window.__calendarForceRouteV7)return;window.__calendarForceRouteV7=1;const KNOWN=new Set(['overview','calendar']);function visible(el){if(!el)return false;const s=getComputedStyle(el),r=el.getBoundingClientRect();return s.display!=='none'&&s.visibility!=='hidden'&&r.width>0&&r.height>0}function detect(){const heads=[...document.querySelectorAll('h1,h2,.page-title,[data-page-title]')].filter(visible);for(const h of heads){const t=String(h.textContent||'').replace(/\s+/g,' ').trim().toLowerCase();if(KNOWN.has(t))return t}const active=[...document.querySelectorAll('[aria-current="page"],.active,[class*="active"]')].filter(visible);for(const el of active){const t=String(el.textContent||'').replace(/\s+/g,' ').trim().toLowerCase();if(KNOWN.has(t))return t}return''}function enforce(){const r=detect();if(!r)return;window.state=window.state||{};if(String(window.state.route||'').toLowerCase()!==r)window.state.route=r;document.documentElement.dataset.posVisibleRoute=r;document.documentElement.dataset.posCalendarBuild='v18'}setInterval(enforce,120);new MutationObserver(enforce).observe(document.documentElement,{subtree:true,childList:true,characterData:true});window.addEventListener('focus',enforce);document.addEventListener('visibilitychange',()=>{if(!document.hidden)enforce()});setTimeout(enforce,50)})();