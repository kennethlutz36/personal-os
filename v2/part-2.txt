?new Date(data.logged_at).toISOString():new Date().toISOString(),meal_type:data.meal_type||null,meal_name:data.meal_name||null,description:data.meal_name||data.description||'Meal',portion:data.portion||null,calories:data.calories?Number(data.calories):null,protein_g:data.protein?Number(data.protein):null,carbs_g:data.carbs?Number(data.carbs):null,fat_g:data.fat?Number(data.fat):null,source_system:'personal_os'};const {error}=await ds.client.from('food_logs').insert(row);if(error)throw error;await loadV2();render()}
  async function logWeight(weight){const u=await auth();const {error}=await ds.client.from('health_daily').upsert({user_id:u.id,metric_date:today(),weight:Number(weight),source_system:'personal_os'},{onConflict:'user_id,metric_date,source_system'});if(error)throw error;await loadV2();render()}
  async function updateWorkout(id,data){const u=await auth();const st=data.started_at?new Date(data.started_at):null,dur=data.duration?Number(data.duration):null;const patch={title:data.title||null,workout_type:data.workout_type||'training',duration_minutes:dur,strain:data.strain?Number(data.strain):null,calories_burned:data.calories?Number(data.calories):null,notes:data.notes||null};if(st){patch.started_at=st.toISOString();patch.ended_at=dur?new Date(st.getTime()+dur*60000).toISOString():null}const {error}=await ds.client.from('workout_logs').update(patch).eq('id',id).eq('user_id',u.id).eq('source_system','personal_os');if(error)throw error;await loadV2();render()}
  async function addWorkout(data){const u=await auth();const st=new Date(data.started_at||new Date()),dur=Number(data.duration||0)||null;const en=dur?new Date(st.getTime()+dur*60000):null;const row={user_id:u.id,started_at:st.toISOString(),ended_at:en?.toISOString()||null,workout_type:data.workout_type||'training',title:data.title||data.workout_type||'Workout',duration_minutes:dur,strain:data.strain?Number(data.strain):null,calories_burned:data.calories?Number(data.calories):null,notes:data.notes||null,source_system:'personal_os'};const {error}=await ds.client.from('workout_logs').insert(row);if(error)throw error;await loadV2();render()}
  async function addSupplement(data){const u=await auth();const doseNum=String(data.dose||'').match(/^([0-9.]+)/)?.[1];const row={user_id:u.id,name:data.name.trim(),dose:doseNum?Number(doseNum):null,dose_text:data.dose||null,frequency:data.frequency,timing:data.timing||null,active:true,reminder_enabled:false,calendar_area:'Personal'};const {error}=await ds.client.from('supplements').insert(row);if(error)throw error;await loadV2();render()}
  async function saveHealthGoals(data){const u=await auth();const row={user_id:u.id,daily_calorie_target:data.calories?Number(data.calories):null,daily_protein_target_g:data.protein?Number(data.protein):null,weight_goal:data.weight?Number(data.weight):null,updated_at:new Date().toISOString()};const {error}=await ds.client.from('health_goals').upsert(row);if(error)throw error;await loadV2();render()}
  async function addLifeItem(data){const u=await auth();const {error}=await ds.client.from('life_items').insert({user_id:u.id,category:data.category,title:data.title.trim(),details:data.details||null,item_date:data.item_date||null,recurrence:data.recurrence||null,status:'open',notes:data.notes||null});if(error)throw error;await loadV2();render()}
  async function updateLifeItem(id,patch){const u=await auth();const {error}=await ds.client.from('life_items').update({...patch,updated_at:new Date().toISOString()}).eq('id',id).eq('user_id',u.id);if(error)throw error;await loadV2();render()}
  async function addMoment(data){const u=await auth();const {error}=await ds.client.from('life_moments').insert({user_id:u.id,happened_at:data.happened_at?new Date(data.happened_at).toISOString():new Date().toISOString(),title:data.title.trim(),details:data.details||null,tags:(data.tags||'').split(',').map(x=>x.trim()).filter(Boolean)});if(error)throw error;await loadV2();render()}
  async function saveBudget(category,limit){const {error}=await ds.client.from('finance_budget_allocations').upsert({owner_email:OWNER,category,monthly_limit:Number(limit)||0,active:true,updated_at:new Date().toISOString()},{onConflict:'owner_email,category'});if(error)throw error;await loadV2();render()}

  function quickCreate(kind='choose'){
    if(kind==='choose'){
      const {root}=modal('Quick create',`<div class="v2-grid two">${[['master','☑','Master checklist','Unscheduled checklist item'],['scheduled','⏱','Scheduled task','Task with date & time'],['event','▦','Calendar event','Personal OS calendar event'],['meal','◉','Meal','Name, portion, calories'],['weight','↕','Weight','Log today’s weight'],['supplement','✦','Supplement','Name, dose, frequency, timing'],['workout','⌁','Workout','Manual training log'],['moment','✧','Life moment','Timestamp a memory or event']].map(([k,ic,t,s])=>`<button class="v2-workspace" style="--area:#6478eb;text-align:left" data-quick-kind="${k}"><span class="go">${ic}</span><b>${t}</b><strong style="font-size:13px;margin-top:12px">＋ Add</strong><span>${s}</span></button>`).join('')}</div>`);
      root.querySelectorAll('[data-quick-kind]').forEach(b=>b.onclick=()=>{root.remove();quickCreate(b.dataset.quickKind)});return;
    }
    if(kind==='master'||kind==='scheduled')return taskForm(kind);
    if(kind==='event')return eventForm();if(kind==='meal')return mealForm();if(kind==='weight')return weightForm();if(kind==='supplement')return supplementForm();if(kind==='workout')return workoutForm();if(kind==='moment')return momentForm();
  }
  function taskForm(kind='master',task=null){const scheduled=kind==='scheduled';const {root,close}=modal(task?'Edit task':scheduled?'Add scheduled task':'Add master checklist item',`<form id="v2TaskForm" class="v2-form"><div class="full"><label>Task</label><input name="title" required value="${esc(task?.title||'')}" placeholder="What needs to happen?"></div><div><label>Area</label><select name="area">${AREAS.map(a=>`<option ${task?.area===a?'selected':''}>${a}</option>`).join('')}</select></div><div><label>Priority</label><select name="priority">${['P1','P2','P3'].map(p=>`<option ${String(task?.priority||'P2')===p?'selected':''}>${p}</option>`).join('')}</select></div><div class="full"><label>Project / context</label><input name="project" value="${esc(task?.project||'')}" placeholder="Optional"></div>${scheduled?`<div class="full"><label>Date & time</label><input type="datetime-local" name="due_at" required value="${task?.due_at?localInput(task.due_at):''}"></div>`:''}<div class="full"><label>Notes</label><textarea name="notes" placeholder="Optional context">${esc(task?.notes||'')}</textarea></div><div class="v2-form-actions"><button type="button" class="v2-btn" data-v2-close>Cancel</button><button class="v2-btn primary" type="submit">${task?'Save changes':'Create'}</button></div></form>`);root.querySelector('[data-v2-close]').onclick=close;root.querySelector('#v2TaskForm').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget),data=Object.fromEntries(f.entries());try{if(task)await updateTask(task.id,{title:data.title,area:data.area,priority:data.priority,project:data.project||null,notes:data.notes||null,...(scheduled?{due_at:new Date(data.due_at).toISOString(),due_date:isoDate(new Date(data.due_at)),task_kind:'scheduled',status:'Scheduled'}:{task_kind:'master',due_at:null,due_date:null})});else await createTask({...data,kind});close();toast(task?'Task updated':'Task created')}catch(err){toast(err.message||'Unable to save task')}}}
  function eventForm(event=null){const start=event?.starts_at?localInput(event.starts_at):localInput(new Date(Date.now()+60*60000)),end=event?.ends_at?localInput(event.ends_at):localInput(new Date(Date.now()+120*60000));const {root,close}=modal(event?'Edit calendar event':'Add calendar event',`<form id="v2EventForm" class="v2-form"><div class="full"><label>Event title</label><input name="title" required value="${esc(event?.title||'')}"></div><div><label>Area</label><select name="area">${AREAS.map(a=>`<option ${event?.area===a?'selected':''}>${a}</option>`).join('')}</select></div><div><label>Location</label><input name="location" value="${esc(event?.location||'')}"></div><div><label>Starts</label><input type="datetime-local" name="starts_at" required value="${start}"></div><div><label>Ends</label><input type="datetime-local" name="ends_at" required value="${end}"></div><div class="full"><label>Notes</label><textarea name="notes">${esc(event?.notes||'')}</textarea></div><div class="v2-form-actions"><button type="button" class="v2-btn" data-v2-close>Cancel</button><button class="v2-btn primary" type="submit">${event?'Save':'Add event'}</button></div></form>`);root.querySelector('[data-v2-close]').onclick=close;root.querySelector('#v2EventForm').onsubmit=async e=>{e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget).entries());try{if(event)await updateEvent(event.id,{title:data.title,area:data.area,location:data.location||null,notes:data.notes||null,starts_at:new Date(data.starts_at).toISOString(),ends_at:new Date(data.ends_at).toISOString()});else await createEvent(data);close();toast(event?'Event updated':'Event added')}catch(err){toast(err.message||'Unable to save event')}}}
  function mealForm(){const {root,close}=modal('Log meal',`<form id="v2MealForm" class="v2-form"><div><label>Meal name</label><input name="meal_name" required placeholder="Chicken bowl"></div><div><label>Portion</label><input name="portion" placeholder="1 bowl / 8 oz / 2 pieces"></div><div><label>Calories</label><input type="number" min="0" step="1" name="calories" required></div><div><label>Meal type</label><select name="meal_type"><option>Breakfast</option><option>Lunch</option><option>Dinner</option><option>Snack</option><option>Other</option></select></div><div><label>Protein (g)</label><input type="number" min="0" step=".1" name="protein"></div><div><label>Carbs (g)</label><input type="number" min="0" step=".1" name="carbs"></div><div><label>Fat (g)</label><input type="number" min="0" step=".1" name="fat"></div><div><label>Time</label><input type="datetime-local" name="logged_at" value="${localInput(new Date())}"></div><div class="v2-form-actions"><button type="button" class="v2-btn" data-v2-close>Cancel</button><button class="v2-btn primary">Log meal</button></div></form>`);root.querySelector('[data-v2-close]').onclick=close;root.querySelector('#v2MealForm').onsubmit=async e=>{e.preventDefault();try{await addMeal(Object.fromEntries(new FormData(e.currentTarget).entries()));close();toast('Meal logged')}catch(err){toast(err.message||'Unable to log meal')}}}
  function weightForm(){const {root,close}=modal('Log weight',`<form id="v2WeightForm" class="v2-form"><div class="full"><label>Weight</label><input type="number" step=".1" min="1" name="weight" required placeholder="lb"></div><div class="v2-form-actions"><button type="button" class="v2-btn" data-v2-close>Cancel</button><button class="v2-btn primary">Save today</button></div></form>`);root.querySelector('[data-v2-close]').onclick=close;root.querySelector('#v2WeightForm').onsubmit=async e=>{e.preventDefault();try{await logWeight(new FormData(e.currentTarget).get('weight'));close();toast('Weight logged')}catch(err){toast(err.message||'Unable to log weight')}}}
  function supplementForm(){const {root,close}=modal('Add supplement',`<form id="v2SuppForm" class="v2-form"><div><label>Name</label><input name="name" required placeholder="Supplement name"></div><div><label>Dose</label><input name="dose" placeholder="e.g. 500 mg"></div><div><label>Frequency</label><select name="frequency"><option value="daily">Daily</option><option value="every_other_day">Every other day</option><option value="weekly">Weekly</option></select></div><div><label>Timing</label><input name="timing" placeholder="Morning / with dinner / bedtime"></div><div class="v2-form-actions"><button type="button" class="v2-btn" data-v2-close>Cancel</button><button c