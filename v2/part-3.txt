lass="v2-btn primary">Add supplement</button></div></form>`);root.querySelector('[data-v2-close]').onclick=close;root.querySelector('#v2SuppForm').onsubmit=async e=>{e.preventDefault();try{await addSupplement(Object.fromEntries(new FormData(e.currentTarget).entries()));close();toast('Supplement added')}catch(err){toast(err.message||'Unable to add supplement')}}}
  function workoutForm(workout=null){const {root,close}=modal(workout?'Edit workout':'Log workout',`<form id="v2WorkoutForm" class="v2-form"><div><label>Workout</label><input name="title" required value="${esc(workout?.title||'')}" placeholder="Upper body / run / tennis"></div><div><label>Type</label><input name="workout_type" value="${esc(workout?.workout_type||'')}" placeholder="strength, run, activity"></div><div><label>Started</label><input type="datetime-local" name="started_at" value="${workout?.started_at?localInput(workout.started_at):localInput(new Date())}"></div><div><label>Duration (min)</label><input type="number" min="0" name="duration" value="${workout?.duration_minutes??''}"></div><div><label>Calories burned</label><input type="number" min="0" name="calories" value="${workout?.calories_burned??''}"></div><div><label>Strain (optional)</label><input type="number" min="0" step=".1" name="strain" value="${workout?.strain??''}"></div><div class="full"><label>Notes</label><textarea name="notes">${esc(workout?.notes||'')}</textarea></div><div class="v2-form-actions"><button type="button" class="v2-btn" data-v2-close>Cancel</button><button class="v2-btn primary">${workout?'Save changes':'Log workout'}</button></div></form>`);root.querySelector('[data-v2-close]').onclick=close;root.querySelector('#v2WorkoutForm').onsubmit=async e=>{e.preventDefault();try{const d=Object.fromEntries(new FormData(e.currentTarget).entries());if(workout)await updateWorkout(workout.id,d);else await addWorkout(d);close();toast(workout?'Workout updated':'Workout logged')}catch(err){toast(err.message||'Unable to save workout')}}}
  function momentForm(){const {root,close}=modal('Log a life moment',`<form id="v2MomentForm" class="v2-form"><div class="full"><label>What happened?</label><input name="title" required placeholder="A moment worth remembering"></div><div><label>When</label><input type="datetime-local" name="happened_at" value="${localInput(new Date())}"></div><div><label>Tags</label><input name="tags" placeholder="family, travel, milestone"></div><div class="full"><label>Details</label><textarea name="details" placeholder="What happened, how it felt, what you want to remember"></textarea></div><div class="v2-form-actions"><button type="button" class="v2-btn" data-v2-close>Cancel</button><button class="v2-btn primary">Save moment</button></div></form>`);root.querySelector('[data-v2-close]').onclick=close;root.querySelector('#v2MomentForm').onsubmit=async e=>{e.preventDefault();try{await addMoment(Object.fromEntries(new FormData(e.currentTarget).entries()));close();toast('Moment saved')}catch(err){toast(err.message||'Unable to save moment')}}}
  function healthGoalsForm(){const g=V.healthGoals||{};const {root,close}=modal('Health tracking goals',`<form id="v2GoalsForm" class="v2-form"><div><label>Daily calories</label><input type="number" name="calories" value="${g.daily_calorie_target??''}" placeholder="Optional"></div><div><label>Daily protein (g)</label><input type="number" name="protein" value="${g.daily_protein_target_g??''}" placeholder="Optional"></div><div><label>Target weight</label><input type="number" step=".1" name="weight" value="${g.weight_goal??''}" placeholder="Optional"></div><div></div><div class="v2-form-actions"><button type="button" class="v2-btn" data-v2-close>Cancel</button><button class="v2-btn primary">Save goals</button></div></form>`);root.querySelector('[data-v2-close]').onclick=close;root.querySelector('#v2GoalsForm').onsubmit=async e=>{e.preventDefault();try{await saveHealthGoals(Object.fromEntries(new FormData(e.currentTarget).entries()));close();toast('Health goals saved')}catch(err){toast(err.message||'Unable to save goals')}}}
  function lifeItemForm(category='other',item=null){const cats=[['admin','Admin'],['home_vehicle','Home & Vehicle'],['relationships','Relationships'],['travel_experiences','Travel & Experiences'],['important_dates','Important Dates'],['other','Other']];const {root,close}=modal(item?'Edit life item':'Add life item',`<form id="v2LifeForm" class="v2-form"><div><label>Category</label><select name="category">${cats.map(([v,l])=>`<option value="${v}" ${(item?.category||category)===v?'selected':''}>${l}</option>`).join('')}</select></div><div><label>Date</label><input type="date" name="item_date" value="${item?.item_date||''}"></div><div class="full"><label>Title</label><input name="title" required value="${esc(item?.title||'')}"></div><div><label>Recurrence</label><select name="recurrence"><option value="">None</option><option value="yearly" ${item?.recurrence==='yearly'?'selected':''}>Yearly</option><option value="monthly" ${item?.recurrence==='monthly'?'selected':''}>Monthly</option><option value="weekly" ${item?.recurrence==='weekly'?'selected':''}>Weekly</option></select></div><div></div><div class="full"><label>Details</label><textarea name="details">${esc(item?.details||'')}</textarea></div><div class="v2-form-actions"><button type="button" class="v2-btn" data-v2-close>Cancel</button><button class="v2-btn primary">${item?'Save changes':'Add item'}</button></div></form>`);root.querySelector('[data-v2-close]').onclick=close;root.querySelector('#v2LifeForm').onsubmit=async e=>{e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget).entries());try{if(item)await updateLifeItem(item.id,{category:data.category,title:data.title,item_date:data.item_date||null,recurrence:data.recurrence||null,details:data.details||null});else await addLifeItem(data);close();toast(item?'Life item updated':'Life item added')}catch(err){toast(err.message||'Unable to save life item')}}}

  function primevaReview(){const rows=(state.primevaApprovals||[]).filter(x=>String(x.state||'pending').toLowerCase()==='pending');modal('Primeva decision queue',`<div class="v2-pills"><span class="v2-pill warn">Read-only in Personal OS</span><span class="v2-pill">${rows.length} pending</span></div><div class="v2-list" style="margin-top:12px">${rows.map(x=>`<div class="v2-row"><div><b>${esc(x.title)}</b><small>${esc(x.brand||'Primeva')} · ${esc(x.summary||'No summary')}</small></div><span class="meta">${esc(x.external_id||'')}</span></div>`).join('')||'<div class="v2-sub">No pending Primeva approvals.</div>'}</div><p class="v2-sub" style="margin-top:12px">Approvals, sends, publishing, and spending remain inside Primeva.</p>`)}
  function openEmail(){if(V.emailTarget){V.emailTarget.click();return}const t=document.getElementById('emailNavItem');if(t){V.emailTarget=t;t.click();return}toast('Email is loading — try again in a moment')}
  function openLink(url){if(url)window.open(url,'_blank','noopener');else toast('Production link is not configured yet')}

  function deterministicActions(){
    const pending=(state.primevaApprovals||[]).filter(x=>String(x.state||'pending').toLowerCase()==='pending').length;
    const now=Date.now();const tasks=[...V.localTasks,...V.externalTasks].filter(x=>!isDone(x)).sort((a,b)=>new Date(a.due_at||a.due_date||'2999-01-01')-new Date(b.due_at||b.due_date||'2999-01-01'));
    const emails=emailThreadCount('needs_reply');const out=[];
    if(pending)out.push({title:`Review ${pending} Primeva decisions`,why:'Human-gated queue is waiting.',route:'primeva',urgency:'now'});
    if(tasks[0])out.push({title:tasks[0].title,why:tasks[0].due_at?`Due ${dt(tasks[0].due_at)}`:'Highest current task priority.',route:'tasks',urgency:'today'});
    if(emails)out.push({title:`Reply to ${emails} email threads`,why:'Messages are waiting for a response.',route:'email',urgency:'today'});
    return out.slice(0,3);
  }
  function overviewPageV2(){
    const ai=V.ai.overview;const acts=(ai?.actions?.length?ai.actions:deterministicActions()).slice(0,3);
    const pending=(state.primevaApprovals||[]).filter(x=>String(x.state||'pending').toLowerCase()==='pending').length;
    const counts={};AREAS.forEach(a=>counts[a]=[...V.localTasks,...V.externalTasks].filter(x=>x.area===a&&!isDone(x)).length);
    const wh=V.healthDaily.filter(x=>x.source_system==='whoop').slice(-1)[0];const prior=V.healthDaily.filter(x=>x.source_system==='whoop'&&x.recovery_score!=null).slice(-7,-1);const br=avg(prior,'recovery_score');
    const currentEnd=V.spend.map(x=>x.period_end).sort().pop();const curr=V.spend.filter(x=>x.period_end===currentEnd);const mtd=sum(curr,'amount');const top=curr.slice().sort((a,b)=>num(b.amount)-num(a.amount))[0];
    const nextLife=V.lifeItems.filter(x=>x.status==='open'&&x.item_date).slice().sort((a,b)=>String(a.item_date).localeCompare(String(b.item_date)))[0];
    const upcoming=V.events.filter(e=>new Date(e.starts_at)>=new Date()).slice(0,6);const needs=emailThreadCount('needs_reply'),waiting=emailThreadCount('waiting_on');
    const healthVals=V.healthDaily.filter(x=>x.source_system==='whoop'&&x.recovery_score!=null).slice(-10).map(x=>num(x.recovery_score));const spendTop=curr.slice().sort((a,b)=>num(b.amount)-num(a.amount)).slice(0,6);
    return `<div class="v2-stack">
      <section class="v2-card v2-hero v2-pad"><div class="v2-head"><div><div class="v2-kicker">AI COMMAND CENTER · LIVE PRIORITIES</div><h2>${esc(ai?.headline||'Best order of attack')}</h2><div class="v2-sub" style="margin-top:5px">${esc(ai?.summary||'Personal OS is ranking tasks, calendar, email, Primeva decisions, finance and readiness into the next best moves.')}</div></div><div class="v2-pills"><span class="v2-pill good">Live · synced</span><button class="v2-btn" data-ai-refresh="overview">↻ AI</button></div></div><div class="v2-actions">${acts.map((x,i)=>`<button class="v2-action" data-overview-action="${i}"><b>${i+1}. ${esc(x.title)}</b><span>${esc(x.why||'Open the relevant workspace and act.')}</span><em>${esc(x.urgency||'today')}</em></button>`).join('')||'<div class="v2-sub">No immediate actions detected.</div>'}</div></section>
      <section><div class="v2-head" style="margin:0 2px 8px"><div><div class="v2-kicker">WORKSPACES</div><h2 style="font-size:14px">Jump to source or work area</h2></div><button class="v2-btn primary" data-v2-quick>＋ Quick Add</button></div><div class="v2-workspaces">${[
        ['Three Rivers',counts['Three Rivers'],'Open territory dashboard →','three_rivers'],['Primeva Labs',counts['Primeva Labs'],`${pending} Primeva decisions · review queue →`,'primeva'],['Primeva Health',counts['Primeva Health'],`${pending} Primeva decisions · review queue →`,'primeva'],['Personal',counts.Personal,'Open personal workspace →','personal']
      ].map(([a,c,s,k])=>`<button class="v2-workspace" style="--area:${C[a]}" data-workspace="${k}"><span class="go">↗</span><b>${a}</b><strong>${c}</strong><span>open items</span><span style="margin-top:7px;color:${C[a]}">${s}</span></button>`).join('')}</div></section>
      <section class="v2-grid three">
        <article class="v2-card v2-pad v2-signal health"><div class="v2-head"><div><div class="v2-kicker">HEALTH SIGNALS</div><h3>Readiness & fuel</h3></div><button class="v2-btn small" data-route="health">Open →</button></div><div style="display:flex;align-items:end;gap:8px;margin-top:10px"><span class="big" style="color:#14966e">${wh?.recovery_score!=null?Math.round(wh.recovery_score)+'%':'—'}</span><span class="v2-sub">recovery${br?` · ${Math.round(num(wh?.recovery_score)-br)>=0?'+':''}${Math.round(num(wh?.recovery_score)-br)} vs 6-day avg`:''}</span></div><div class="v2-kpis"><div class="v2-kpi"><span>HRV</span><b>${wh?.hrv_ms?Math.round(wh.hrv_ms)+' ms':'—'}</b></div><div class="v2-kpi"><span>Sleep</span><b>${wh?.sleep_hours?num(wh.sleep_hours).toFixed(1)+'h':'—'}</b></div><div class="v2-kpi"><span>Strain</span><b>${wh?.strain?num(wh.strain).toFixed(1):