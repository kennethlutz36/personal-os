'—'}</b></div></div><div class="v2-mini-bars">${healthVals.map(v=>`<i style="height:${clamp(v,8,100)}%;--bar:#19ad82"></i>`).join('')}</div></article>
        <article class="v2-card v2-pad v2-signal finance"><div class="v2-head"><div><div class="v2-kicker">FINANCE SIGNALS</div><h3>Cash flow & pressure</h3></div><button class="v2-btn small" data-route="finance">Open →</button></div><div style="display:flex;align-items:end;gap:8px;margin-top:10px"><span class="big" style="color:#3277d6">${money(mtd)}</span><span class="v2-sub">posted category spend · current snapshot</span></div><div class="v2-kpis"><div class="v2-kpi"><span>Top category</span><b>${top?fmtCat(top.category):'—'}</b></div><div class="v2-kpi"><span>Known util.</span><b>${(()=>{const cs=(state.financeAccounts||[]).filter(a=>a.account_type==='credit'),lim=sum(cs,'credit_limit'),bal=sum(cs,'current_balance');return lim?Math.round(bal/lim*100)+'%':'—'})()}</b></div><div class="v2-kpi"><span>Recurring</span><b>${money((state.recurringFinance||[]).reduce((s,r)=>s+monthlyRecurring(r),0))}</b></div></div><div class="v2-mini-bars">${spendTop.map((x,i)=>`<i style="height:${Math.max(12,num(x.amount)/Math.max(1,num(spendTop[0]?.amount))*100)}%;--bar:${['#3b82f6','#5f7ee8','#6d6edc','#4da7d9','#42b4c7','#5fc3b3'][i]}"></i>`).join('')}</div></article>
        <article class="v2-card v2-pad v2-signal life"><div class="v2-head"><div><div class="v2-kicker">LIFE / SYSTEM</div><h3>Open loops & memory</h3></div><button class="v2-btn small" data-route="life">Open →</button></div><div style="display:flex;align-items:end;gap:8px;margin-top:10px"><span class="big" style="color:#7455dc">${V.lifeItems.filter(x=>x.status==='open').length}</span><span class="v2-sub">life items tracked</span></div><div class="v2-kpis"><div class="v2-kpi"><span>Email replies</span><b>${needs}</b></div><div class="v2-kpi"><span>Waiting</span><b>${waiting}</b></div><div class="v2-kpi"><span>Next date</span><b>${nextLife?dte(nextLife.item_date):'—'}</b></div></div><div class="v2-list" style="margin-top:10px">${V.lifeMoments.slice(0,1).map(m=>`<div class="v2-row"><div><b>Latest memory · ${esc(m.title)}</b><small>${dt(m.happened_at)}</small></div></div>`).join('')||'<div class="v2-sub">Use + to log a life moment.</div>'}</div></article>
      </section>
      <section class="v2-card v2-pad"><div class="v2-head"><div><div class="v2-kicker">NEXT ON CALENDAR</div><h3>What the next few days actually contain</h3></div><button class="v2-btn small" data-route="calendar">Full calendar →</button></div><div class="v2-grid three" style="margin-top:10px">${upcoming.map(e=>`<button class="v2-row" data-event-id="${e.id}" style="text-align:left;border-left:3px solid ${C[e.area]||'#8290a4'}"><div><b>${esc(e.title)}</b><small>${esc(e.area)} · ${dt(e.starts_at)}</small></div></button>`).join('')||'<div class="v2-sub">No upcoming events loaded.</div>'}</div></section>
    </div>`;
  }

  function taskCard(t){const local=isPersonal(t),due=t.due_at||t.due_date;return `<article class="v2-task ${String(t.priority||'P2').toLowerCase()}"><b>${esc(t.title)}</b><small>${esc(t.project||t.source_payload?.account_name||'')}${due?` · ${dt(due)}`:''}</small><div class="v2-task-foot"><span class="v2-source">${local?'Personal OS':String(t.source_system||'source').replaceAll('_',' ')}</span><div class="v2-task-buttons">${local?`<button class="v2-btn small" data-task-edit="${t.id}">Edit</button><button class="v2-btn small primary" data-task-done="${t.id}">✓</button>`:`<button class="v2-btn small" data-task-source="${esc(t.source_system||'')}">Source</button>`}</div></div></article>`}
  function tasksPageV2(){const all=[...V.localTasks,...V.externalTasks].filter(x=>!isDone(x));return `<div class="v2-stack"><div class="v2-head"><div><div class="v2-kicker">EXECUTION</div><h2>Task Command Center</h2><div class="v2-sub">Master checklist + scheduled execution, separated by area and source.</div></div><button class="v2-btn primary" data-v2-quick>＋ Add Task</button></div>${aiBlock('tasks')}<div class="v2-task-board">${AREAS.map(a=>{const rows=all.filter(x=>x.area===a),master=rows.filter(x=>!(x.task_kind==='scheduled'||x.due_at||x.due_date)),scheduled=rows.filter(x=>!master.includes(x));return `<section class="v2-task-col" style="--area:${C[a]};--tint:${a==='Three Rivers'?'#fff8e8':a==='Primeva Labs'?'#eef6ff':a==='Primeva Health'?'#ecfaf5':'#f5f0ff'}"><div class="v2-task-col-head"><b>${a.toUpperCase()}</b><div style="display:flex;align-items:center;gap:5px"><span class="v2-badge">${rows.length} open</span><button class="v2-btn small" data-area-source="${areaSlug(a)}">↗</button></div></div><div class="v2-task-group"><div class="v2-task-group-title">Master checklist</div>${master.map(taskCard).join('')||'<div class="v2-sub">No unscheduled items.</div>'}</div><div class="v2-task-group"><div class="v2-task-group-title">Scheduled</div>${scheduled.map(taskCard).join('')||'<div class="v2-sub">No scheduled items.</div>'}</div></section>`}).join('')}</div></div>`}

  function weekStart(){const d=new Date();d.setHours(0,0,0,0);const dow=(d.getDay()+6)%7;d.setDate(d.getDate()-dow+V.calendarOffset*7);return d}
  function calendarPageV2(){const start=weekStart(),days=Array.from({length:7},(_,i)=>{const d=new Date(start);d.setDate(start.getDate()+i);return d}),end=new Date(start);end.setDate(end.getDate()+7);const rows=V.events.filter(e=>new Date(e.starts_at)>=start&&new Date(e.starts_at)<end);return `<div class="v2-stack"><div class="v2-head"><div><div class="v2-kicker">WEEK PLANNER</div><h2>${start.toLocaleDateString('en-US',{month:'long',day:'numeric'})} – ${new Date(end.getTime()-86400000).toLocaleDateString('en-US',{month:'long',day:'numeric'})}</h2><div class="v2-sub">Synced events are read-only. Personal OS events can be added and edited here.</div></div><div class="v2-weeknav"><button class="v2-btn" data-cal-nav="-1">←</button><button class="v2-btn" data-cal-nav="0">Today</button><button class="v2-btn" data-cal-nav="1">→</button><button class="v2-btn primary" data-add-event>＋ Event</button></div></div><div class="v2-week">${days.map(d=>{const dsx=isoDate(d),ev=rows.filter(e=>isoDate(new Date(e.starts_at))===dsx).sort((a,b)=>new Date(a.starts_at)-new Date(b.starts_at));return `<section class="v2-day ${dsx===today()?'today':''}"><div class="v2-day-head"><b>${d.toLocaleDateString('en-US',{weekday:'short'}).toUpperCase()}</b><span>${d.getDate()}</span></div>${ev.map(e=>`<article class="v2-event" style="--area:${C[e.area]||'#77889d'}" data-event-id="${e.id}"><b>${esc(e.title)}</b><span>${new Date(e.starts_at).toLocaleTimeString('en-US',{hour:'numeric',minute:'2-digit'})} · ${esc(e.area)}</span>${isPersonal(e)?'<span class="v2-badge local" style="margin-top:4px">editable</span>':'<span class="v2-badge readonly" style="margin-top:4px">synced</span>'}</article>`).join('')||'<div class="v2-day-empty">No events</div>'}</section>`}).join('')}</div></div>`}

  function financeModel(){const ac=state.financeAccounts||[],cash=ac.filter(a=>['depository','checking','savings'].includes(String(a.account_type||a.account_subtype))).reduce((s,a)=>s+Math.max(0,num(a.current_balance)),0),invest=(state.investmentHoldings||[]).reduce((s,h)=>s+num(h.institution_value),0)||ac.filter(a=>a.account_type==='investment').reduce((s,a)=>s+num(a.current_balance),0);const credit=ac.filter(a=>a.account_type==='credit').reduce((s,a)=>s+Math.max(0,num(a.current_balance)),0);const liabilities=(state.liabilities||[]).reduce((s,l)=>s+Math.max(0,num(l.current_balance)),0);const manual=(state.assets||[]).filter(a=>String(a.asset_category||'').includes('loan')||String(a.asset_category||'').includes('debt')).reduce((s,a)=>s+Math.max(0,num(a.current_worth)),0);const assets=(state.assets||[]).filter(a=>!String(a.asset_category||'').includes('loan')&&!String(a.asset_category||'').includes('debt')).reduce((s,a)=>s+Math.max(0,num(a.current_worth)),0);const net=cash+invest+assets-credit-liabilities-manual;return{cash,invest,credit,liabilities,manual,assets,net}}
  function budgetModal(){const cats=['housing','transportation','bills_utilities','groceries','dining_drinks','shopping','travel','entertainment','health_wellness','services'];const {root,close}=modal('Monthly budget allocation',`<p class="v2-sub">These are planning limits only. Saving changes does not move money between accounts.</p><div class="v2-list" style="margin-top:12px">${cats.map(c=>{const b=V.budgets.find(x=>x.category===c);return `<div class="v2-row"><div style="min-width:150px"><b>${fmtCat(c)}</b><small>Monthly planning limit</small></div><div style="display:flex;align-items:center;gap:6px"><input class="v2-input" style="width:110px" type="number" min="0" step="10" data-budget-cat="${c}" value="${b?.monthly_limit??''}" placeholder="0"><button class="v2-btn small" data-budget-save="${c}">Save</button></div></div>`}).join('')}</div>`);root.querySelectorAll('[data-budget-save]').forEach(b=>b.onclick=async()=>{const cat=b.dataset.budgetSave,inp=root.querySelector(`[data-budget-cat="${cat}"]`);try{await saveBudget(cat,inp.value);toast(`${fmtCat(cat)} budget saved`)}catch(e){toast(e.message)}});}
  function financePageV2(){const f=financeModel(),latestEnd=V.spend.map(x=>x.period_end).sort().pop(),curr=V.spend.filter(x=>x.period_end===latestEnd),priorEnd=V.spend.map(x=>x.period_end).filter(x=>x!==latestEnd).sort().pop(),prev=V.spend.filter(x=>x.period_end===priorEnd),mtd=sum(curr,'amount'),income=sum(V.income.filter(x=>x.period_end===latestEnd),'amount'),top=curr.slice().sort((a,b)=>num(b.amount)-num(a.amount)),max=Math.max(1,...top.map(x=>num(x.amount)));const util=(()=>{const cs=(state.financeAccounts||[]).filter(a=>a.account_type==='credit'),l=sum(cs,'credit_limit'),b=sum(cs,'current_balance');return l?b/l*100:null})();const monthlyBud=sum(V.budgets,'monthly_limit');return `<div class="v2-stack"><div class="v2-head"><div><div class="v2-kicker">MONEY SYSTEM</div><h2>Finance Command Center</h2><div class="v2-sub">Spending intelligence, debt pressure, recurring obligations and editable budget planning.</div></div><div style="display:flex;gap:6px"><button class="v2-btn" data-budget-open>Reallocate budget</button><button class="v2-btn primary" data-ai-refresh="finance">↻ AI Analysis</button></div></div>${aiBlock('finance','finance')}<div class="v2-finance-kpis"><div class="v2-finance-kpi"><span>Liquid cash</span><b>${money(f.cash)}</b><em>connected deposits</em></div><div class="v2-finance-kpi"><span>Investments</span><b>${money(f.invest)}</b><em>current holdings</em></div><div class="v2-finance-kpi"><span>Tracked debt</span><b>${money(f.credit+f.liabilities+f.manual)}</b><em>credit + loans + manual</em></div><div class="v2-finance-kpi"><span>Net position</span><b>${money(f.net)}</b><em>estimated</em></div><div class="v2-finance-kpi"><span>Current spend</span><b>${money(mtd)}</b><em>posted category snapshot</em></div><div class="v2-finance-kpi"><span>Confirmed inflow</span><b>${money(income)}</b><em>salary/rental only</em></div></div><div class="v2-grid two"><article class="v2-chart"><div class="v2-chart-head"><div><div class="v2-kicker">SPEND BY CATEGORY</div><h3>Where the money went</h3></div><span class="v2-pill">through ${dte(latestEnd)}</span></div><div class="v2-hbars">${top.slice(0,9).map((x,i)=>`<div class="v2-hbar-row"><span>${fmtCat(x.category)}</span><div class="v2-hbar-track"><div class="v2-hbar-fill" style="width:${num(x.amount)/max*100}%;background:${['#456eea','#3d91df','#35abc9','#47b89c','#8c72db','#d68a4b','#e19f42','#db6c79','#6e91b9'][i]}"></div></div><b>${money(x.amount)}</b></div>`).join('')}</div></article><article class="v2-chart"><div class="v2-chart-head"><div><div class="v2-kicker">MONTH COMPARISON</div><h3>Current snapshot vs July</h3></div><span class="v2-pill ${mtd<sum(prev,'amount')?'good':'warn'}">${money(mtd)} vs ${money(sum(prev,'amount'))}</span></div><div class="v2-bars">${['housing','transportation','travel','shopping','dining_drinks','bills_utilities'].map(c=>{