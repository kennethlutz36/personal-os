const a=num(curr.find(x=>x.category===c)?.amount),p=num(prev.find(x=>x.category===c)?.amount),mx=Math.max(1,a,p);return `<div class="v2-bar-wrap"><div style="display:flex;align-items:flex-end;justify-content:center;gap:3px;height:110px"><div class="v2-bar" style="height:${a/mx*100}%;width:13px;--bar1:#506fea;--bar2:#7d94ef"></div><div class="v2-bar" style="height:${p/mx*100}%;width:13px;--bar1:#b7c0d2;--bar2:#d5dbe6"></div></div><div class="v2-bar-label">${fmtCat(c)}</div></div>`}).join('')}</div><div class="v2-sub" style="margin-top:8px">Blue = current snapshot · gray = July full month. Current month is still in progress.</div></article></div><div class="v2-grid two"><article class="v2-card v2-pad"><div class="v2-head"><div><div class="v2-kicker">BUDGET CONTROL</div><h3>Planning limits</h3></div><button class="v2-btn small" data-budget-open>Edit</button></div><div class="v2-kpis"><div class="v2-kpi"><span>Budgeted</span><b>${monthlyBud?money(monthlyBud):'Not set'}</b></div><div class="v2-kpi"><span>Known util.</span><b>${util!=null?Math.round(util)+'%':'—'}</b></div><div class="v2-kpi"><span>Top spend</span><b>${top[0]?fmtCat(top[0].category):'—'}</b></div></div><div class="v2-list" style="margin-top:10px">${V.budgets.slice(0,6).map(b=>`<div class="v2-row"><div><b>${fmtCat(b.category)}</b></div><span class="meta">${money(b.monthly_limit)}</span></div>`).join('')||'<div class="v2-sub">Set planning limits to track category headroom. No bank transfers occur here.</div>'}</div></article><article class="v2-card v2-pad"><div class="v2-head"><div><div class="v2-kicker">DEBT / APR PRESSURE</div><h3>What costs the most to carry</h3></div></div><div class="v2-list" style="margin-top:10px">${(state.liabilities||[]).slice().sort((a,b)=>num(b.interest_rate)-num(a.interest_rate)).slice(0,7).map(l=>`<div class="v2-row"><div><b>${esc(l.account_name||l.liability_type||'Liability')}</b><small>${l.next_payment_due?`Due ${dte(l.next_payment_due)}`:''}</small></div><div class="meta">${l.current_balance?money(l.current_balance):''}${l.interest_rate?` · ${num(l.interest_rate).toFixed(2)}%`:''}</div></div>`).join('')||'<div class="v2-sub">Liability terms will populate from connected finance data.</div>'}</div></article></div><div class="v2-grid two"><article class="v2-card v2-pad"><div class="v2-kicker">UPCOMING RECURRING</div><h3>Next obligations</h3><div class="v2-list" style="margin-top:9px">${(state.recurringFinance||[]).filter(r=>r.predicted_next_date).slice().sort((a,b)=>String(a.predicted_next_date).localeCompare(String(b.predicted_next_date))).slice(0,10).map(r=>`<div class="v2-row"><div><b>${esc(r.merchant_name||r.description||'Recurring')}</b><small>${dte(r.predicted_next_date)} · ${esc(r.frequency||'')}</small></div><span class="meta">${money(Math.abs(num(r.last_amount||r.average_amount)))}</span></div>`).join('')}</div></article><article class="v2-card v2-pad"><div class="v2-kicker">TOP MERCHANTS</div><h3>Current snapshot</h3><div class="v2-list" style="margin-top:9px">${V.merchants.filter(x=>x.period_end===latestEnd).slice().sort((a,b)=>num(b.amount)-num(a.amount)).slice(0,10).map(x=>`<div class="v2-row"><div><b>${esc(x.merchant_name)}</b><small>${x.transaction_count} transaction${x.transaction_count===1?'':'s'}</small></div><span class="meta">${money(x.amount)}</span></div>`).join('')}</div></article></div></div>`}

  function healthCharts(metric,label,color,formatter=v=>String(Math.round(v))){const rows=V.healthDaily.filter(x=>x.source_system==='whoop'&&x[metric]!=null).slice(-14),vals=rows.map(x=>num(x[metric])),mx=Math.max(1,...vals),mn=Math.min(...vals);return `<article class="v2-chart"><div class="v2-chart-head"><div><div class="v2-kicker">${label}</div><h3>${vals.length?formatter(vals[vals.length-1]):'—'}</h3></div><span class="v2-pill">14-day</span></div><div class="v2-mini-bars" style="height:88px">${vals.map(v=>`<i style="height:${20+(v-mn)/Math.max(.01,mx-mn)*80}%;--bar:${color}"></i>`).join('')}</div></article>`}
  function healthPageV2(){const who=V.healthDaily.filter(x=>x.source_system==='whoop').slice(-1)[0],todayFood=V.food.filter(x=>String(x.logged_at).slice(0,10)===today()),cal=sum(todayFood,'calories'),protein=sum(todayFood,'protein_g'),goal=num(V.healthGoals?.daily_calorie_target),protGoal=num(V.healthGoals?.daily_protein_target_g),weights=V.healthDaily.filter(x=>x.weight!=null).slice(-20),lastWeight=weights.at(-1)?.weight;return `<div class="v2-stack"><div class="v2-head"><div><div class="v2-kicker">WELLNESS + TRAINING</div><h2>Health Command Center</h2><div class="v2-sub">WHOOP readiness, nutrition, weight, training and your supplement tracker.</div></div><div style="display:flex;gap:6px"><button class="v2-btn" data-health-goals>Goals</button><button class="v2-btn primary" data-ai-refresh="health">↻ AI Review</button></div></div>${aiBlock('health','health')}<section class="v2-card v2-pad"><div style="display:grid;grid-template-columns:125px 1fr;gap:16px;align-items:center"><div class="v2-ring" style="--pct:${clamp(num(who?.recovery_score),0,100)};--ring:#16ad7a"><div><div><b>${who?.recovery_score!=null?Math.round(who.recovery_score)+'%':'—'}</b><span>Recovery</span></div></div></div><div class="v2-health-metrics"><div class="v2-health-metric"><span>Sleep</span><b>${who?.sleep_hours?num(who.sleep_hours).toFixed(1)+'h':'—'}</b></div><div class="v2-health-metric"><span>HRV</span><b>${who?.hrv_ms?Math.round(who.hrv_ms)+' ms':'—'}</b></div><div class="v2-health-metric"><span>RHR</span><b>${who?.resting_hr?Math.round(who.resting_hr)+' bpm':'—'}</b></div><div class="v2-health-metric"><span>Strain</span><b>${who?.strain?num(who.strain).toFixed(1):'—'}</b></div><div class="v2-health-metric"><span>Weight</span><b>${lastWeight?num(lastWeight).toFixed(1):'—'}</b><button class="v2-btn small" style="margin-top:5px" data-quick-kind="weight">＋</button></div></div></div></section><div class="v2-grid four">${healthCharts('recovery_score','RECOVERY','#16ad7a',v=>Math.round(v)+'%')}${healthCharts('hrv_ms','HRV','#3a82dd',v=>Math.round(v)+' ms')}${healthCharts('sleep_hours','SLEEP','#7659d8',v=>v.toFixed(1)+'h')}${healthCharts('strain','STRAIN','#e39b28',v=>v.toFixed(1))}</div><div class="v2-grid two"><article class="v2-card v2-pad"><div class="v2-head"><div><div class="v2-kicker">NUTRITION</div><h3>Today’s intake</h3></div><button class="v2-btn small primary" data-quick-kind="meal">＋ Meal</button></div><div class="v2-kpis"><div class="v2-kpi"><span>Calories</span><b>${Math.round(cal)}${goal?` / ${Math.round(goal)}`:''}</b></div><div class="v2-kpi"><span>Protein</span><b>${Math.round(protein)}g${protGoal?` / ${Math.round(protGoal)}g`:''}</b></div><div class="v2-kpi"><span>Meals</span><b>${todayFood.length}</b></div></div>${goal?`<div class="v2-progress" style="margin-top:10px"><i style="width:${clamp(cal/goal*100,0,100)}%"></i></div>`:''}<div class="v2-list" style="margin-top:10px">${V.food.slice(0,7).map(f=>`<div class="v2-row"><div><b>${esc(f.meal_name||f.description)}</b><small>${esc(f.portion||f.meal_type||'')} · ${dt(f.logged_at)}</small></div><span class="meta">${f.calories?Math.round(f.calories)+' cal':'—'}</span></div>`).join('')||'<div class="v2-sub">No meals logged yet.</div>'}</div></article><article class="v2-card v2-pad"><div class="v2-head"><div><div class="v2-kicker">WEIGHT TREND</div><h3>${lastWeight?num(lastWeight).toFixed(1):'No weight yet'}</h3></div><button class="v2-btn small" data-quick-kind="weight">＋ Weight</button></div><div class="v2-mini-bars" style="height:120px">${(()=>{const vals=weights.map(x=>num(x.weight)),mx=Math.max(1,...vals),mn=Math.min(...vals);return vals.map(v=>`<i style="height:${25+(v-mn)/Math.max(.1,mx-mn)*75}%;--bar:#7c5ce6"></i>`).join('')})()}</div><div class="v2-sub">Weight entries are stored separately from WHOOP metrics for the same day.</div></article></div><div class="v2-grid two"><article class="v2-card v2-pad"><div class="v2-head"><div><div class="v2-kicker">TRAINING</div><h3>Workout log</h3></div><button class="v2-btn small primary" data-quick-kind="workout">＋ Workout</button></div><div class="v2-list" style="margin-top:10px">${V.workouts.slice(0,10).map(w=>`<div class="v2-row"><div><b>${esc(w.title||w.workout_type)}</b><small>${dt(w.started_at)} · ${w.duration_minutes?Math.round(w.duration_minutes)+' min':''}</small></div><div style="display:flex;align-items:center;gap:5px"><span class="v2-badge ${w.source_system==='whoop'?'readonly':'local'}">${w.source_system==='whoop'?'WHOOP · read-only':'Personal · editable'}</span>${w.source_system==='personal_os'?`<button class="v2-btn small" data-workout-edit="${w.id}">Edit</button>`:''}</div></div>`).join('')}</div></article><article class="v2-card v2-pad"><div class="v2-head"><div><div class="v2-kicker">SUPPLEMENT PLAN</div><h3>${V.supplements.filter(x=>x.active).length} active</h3></div><button class="v2-btn small primary" data-quick-kind="supplement">＋ Supplement</button></div><div class="v2-list" style="margin-top:10px">${V.supplements.filter(x=>x.active).map(s=>`<div class="v2-row"><div><b>${esc(s.name)}${s.dose_text?` · ${esc(s.dose_text)}`:s.dose?` · ${esc(s.dose)}${esc(s.unit||'')}`:''}</b><small>${fmtCat(s.frequency||'Flexible')} · ${esc(s.timing||'Flexible timing')}</small></div></div>`).join('')||'<div class="v2-sub">No supplements configured.</div>'}</div></article></div></div>`}

  const lifeCats=[['admin','⌘','Admin'],['home_vehicle','⌂','Home & Vehicle'],['relationships','♡','Relationships'],['travel_experiences','✈','Travel & Experiences'],['important_dates','◫','Important Dates']];
  function lifeCategoryModal(cat){const label=lifeCats.find(x=>x[0]===cat)?.[2]||'Life items',rows=V.lifeItems.filter(x=>x.category===cat&&x.status!=='archived');const {root}=modal(label,`<div class="v2-head"><p class="v2-sub">Add, edit, complete, or maintain important dates in this category.</p><button class="v2-btn primary" data-life-add="${cat}">＋ Add</button></div><div class="v2-list" style="margin-top:12px">${rows.map(x=>`<div class="v2-row"><div><b>${esc(x.title)}</b><small>${x.item_date?dte(x.item_date):'No date'}${x.recurrence?` · ${esc(x.recurrence)}`:''}${x.details?` · ${esc(x.details)}`:''}</small></div><div style="display:flex;gap:4px"><button class="v2-btn small" data-life-edit="${x.id}">Edit</button>${x.status==='open'?`<button class="v2-btn small primary" data-life-done="${x.id}">✓</button>`:''}</div></div>`).join('')||'<div class="v2-sub">Nothing tracked here yet.</div>'}</div>`);root.querySelector('[data-life-add]').onclick=()=>{root.remove();lifeItemForm(cat)};root.querySelectorAll('[data-life-edit]').forEach(b=>b.onclick=()=>{const x=V.lifeItems.find(i=>i.id===b.dataset.lifeEdit);root.remove();lifeItemForm(cat,x)});root.querySelectorAll('[data-life-done]').forEach(b=>b.onclick=async()=>{try{await updateLifeItem(b.dataset.lifeDone,{status:'done'});root.remove();lifeCategoryModal(cat)}catch(e){toast(e.message)}})}
  function lifePageV2(){const open=V.lifeItems.filter(x=>x.status==='open'),upcoming=open.filter(x=>x.item_date).slice().sort((a,b)=>String(a.item_date).localeCompare(String(b.item_date))).slice(0,8);return `<div class="v2-stack"><div class="v2-head"><div><div class="v2-kicker">PERSONAL LIFE OS</div><h2>Life Command Center</h2><div class="v2-sub">Important dates, relationships, home/admin loops, travel and a permanent memory timeline.</div></div><button class="v2-btn primary" data-quick-kind="moment">＋ Log Moment</button></div><section class="v2-card v2-pad" style="background:linear-gradient(135deg,#f8f3ff,#fff 55%,#fff8eb)"><div class="v2-head"><div><div class="v2-kicker">LIFE TIMELINE / MEMORY LOG</div><h3>Capture what happened while it is fresh</h3></div><button class="v2-btn" data-quick-kind="moment">Open full entry</button></div><form id="v2MomentInline" class="v2-moment-entry"><input class="v2-input" type="datetime-local" name="happened_at" value="${localInput(new Date())}"><input class="v2-input" name="title" required placeholder="What happened?"><button c