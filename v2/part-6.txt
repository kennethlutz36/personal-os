lass="v2-btn primary">Save moment</button></form><div class="v2-list" style="margin-top:11px">${V.lifeMoments.slice(0,4).map(m=>`<div class="v2-row"><div><b>${esc(m.title)}</b><small>${dt(m.happened_at)}${m.details?` · ${esc(m.details)}`:''}</small></div></div>`).join('')||'<div class="v2-sub">No memories logged yet.</div>'}</div></section>${aiBlock('life','life')}<div class="v2-life-grid">${lifeCats.map(([k,ic,l])=>{const rows=open.filter(x=>x.category===k),next=rows.filter(x=>x.item_date).sort((a,b)=>String(a.item_date).localeCompare(String(b.item_date)))[0];return `<button class="v2-life-cat" data-life-cat="${k}"><span class="ic">${ic}</span><b>${l}</b><strong>${rows.length}</strong><span>${next?`Next · ${dte(next.item_date)}`:'Click to add / edit'}</span></button>`}).join('')}</div><div class="v2-grid two"><article class="v2-card v2-pad"><div class="v2-kicker">UPCOMING IMPORTANT DATES</div><h3>What is approaching</h3><div class="v2-list" style="margin-top:10px">${upcoming.map(x=>`<div class="v2-row"><div><b>${esc(x.title)}</b><small>${fmtCat(x.category)}${x.recurrence?` · ${esc(x.recurrence)}`:''}</small></div><span class="meta">${dte(x.item_date)}</span></div>`).join('')||'<div class="v2-sub">Add birthdays, renewals, trips, and commitments to make this useful.</div>'}</div></article><article class="v2-card v2-pad"><div class="v2-kicker">RECENT MEMORIES</div><h3>Timeline</h3><div class="v2-list" style="margin-top:10px">${V.lifeMoments.slice(0,8).map(m=>`<div class="v2-row"><div><b>${esc(m.title)}</b><small>${esc(m.details||'')}</small></div><span class="meta">${dte(m.happened_at)}</span></div>`).join('')||'<div class="v2-sub">Your timestamped memories will collect here.</div>'}</div></article></div></div>`}

  function bindV2(){
    document.querySelectorAll('[data-v2-quick]').forEach(b=>b.onclick=()=>quickCreate());document.querySelectorAll('[data-quick-kind]').forEach(b=>b.onclick=()=>quickCreate(b.dataset.quickKind));
    document.querySelectorAll('[data-route]').forEach(b=>b.onclick=()=>{state.route=b.dataset.route;render()});
    document.querySelectorAll('[data-ai-refresh]').forEach(b=>b.onclick=()=>getAI(b.dataset.aiRefresh,true));document.querySelectorAll('[data-ai-action]').forEach(b=>b.onclick=()=>{const [m,i]=b.dataset.aiAction.split(':');handleAI(m,Number(i))});
    document.querySelectorAll('[data-overview-action]').forEach(b=>b.onclick=()=>{const acts=V.ai.overview?.actions?.length?V.ai.overview.actions:deterministicActions(),x=acts[Number(b.dataset.overviewAction)];if(!x)return;if(x.route==='primeva')primevaReview();else if(x.route==='email')openEmail();else if(x.route&&x.route!=='none'){state.route=x.route;render()}});
    document.querySelectorAll('[data-workspace]').forEach(b=>b.onclick=()=>{const k=b.dataset.workspace;if(k==='three_rivers')openLink(link('three_rivers'));else if(k==='primeva')primevaReview();else{state.route='life';render()}});
    document.querySelectorAll('[data-task-done]').forEach(b=>b.onclick=()=>updateTask(b.dataset.taskDone,{status:'Done',completed_at:new Date().toISOString()}).then(()=>toast('Task completed')).catch(e=>toast(e.message)));document.querySelectorAll('[data-task-edit]').forEach(b=>b.onclick=()=>{const t=V.localTasks.find(x=>x.id===b.dataset.taskEdit);if(t)taskForm(t.task_kind||'master',t)});document.querySelectorAll('[data-task-source]').forEach(b=>b.onclick=()=>{if(b.dataset.taskSource==='three_rivers')openLink(link('three_rivers'));else primevaReview()});document.querySelectorAll('[data-area-source]').forEach(b=>b.onclick=()=>{const s=b.dataset.areaSource;if(s==='three')openLink(link('three_rivers'));else if(s==='labs'||s==='health')primevaReview();else{state.route='life';render()}});
    document.querySelectorAll('[data-cal-nav]').forEach(b=>b.onclick=()=>{const n=Number(b.dataset.calNav);V.calendarOffset=n===0?0:V.calendarOffset+n;render()});document.querySelectorAll('[data-add-event]').forEach(b=>b.onclick=()=>eventForm());document.querySelectorAll('[data-event-id]').forEach(b=>b.onclick=()=>{const e=V.events.find(x=>x.id===b.dataset.eventId);if(!e)return;if(isPersonal(e))eventForm(e);else modal('Synced event',`<div class="v2-row"><div><b>${esc(e.title)}</b><small>${esc(e.area)} · ${dt(e.starts_at)}${e.location?` · ${esc(e.location)}`:''}</small></div><span class="v2-badge readonly">read-only</span></div><p class="v2-sub" style="margin-top:10px">Edit this event in its source calendar.</p>`) });
    document.querySelectorAll('[data-budget-open]').forEach(b=>b.onclick=budgetModal);document.querySelectorAll('[data-health-goals]').forEach(b=>b.onclick=healthGoalsForm);
    document.querySelectorAll('[data-workout-edit]').forEach(b=>b.onclick=()=>{const w=V.workouts.find(x=>x.id===b.dataset.workoutEdit);if(w)workoutForm(w)});
    document.querySelectorAll('[data-life-cat]').forEach(b=>b.onclick=()=>lifeCategoryModal(b.dataset.lifeCat));const mf=document.getElementById('v2MomentInline');if(mf)mf.onsubmit=async e=>{e.preventDefault();try{await addMoment(Object.fromEntries(new FormData(e.currentTarget).entries()));toast('Moment saved')}catch(err){toast(err.message)}};
    const baseQuick=[...document.querySelectorAll('button,a,[role="button"]')].find(x=>x.textContent?.includes('Quick Add'));if(baseQuick&&!baseQuick.dataset.v2Bound){baseQuick.dataset.v2Bound='1';baseQuick.onclick=e=>{e.preventDefault();quickCreate()}}
    ensureQuickButton();ensureEmailNav();
    const route=String(state.route||'overview');if(['overview','tasks','finance','health','life'].includes(route)&&!V.ai[route]&&!V.aiLoading[route])getAI(route,false);
  }
  function ensureQuickButton(){let b=document.getElementById('v2FloatingAdd');if(!b){b=document.createElement('button');b.id='v2FloatingAdd';b.className='v2-top-add';b.title='Quick create';b.textContent='＋';b.onclick=()=>quickCreate();document.body.appendChild(b)}}
  function captureEmail(){const t=document.getElementById('emailNavItem');if(t&&!V.emailTarget)V.emailTarget=t}
  function ensureEmailNav(){captureEmail();const sidebar=document.querySelector('.nav')?.parentElement||document.querySelector('.sidebar');if(!sidebar)return;if([...sidebar.querySelectorAll('button,a,[role="button"]')].some(x=>x.textContent?.trim()==='Email'&&x.id!=='emailNavItem'))return;const cal=[...sidebar.querySelectorAll('button,a,[role="button"]')].find(x=>x.textContent?.trim()==='Calendar');if(!cal)return;const n=cal.cloneNode(true);n.id='v2EmailNav';const walker=document.createTreeWalker(n,NodeFilter.SHOW_TEXT);let x;while((x=walker.nextNode()))if(x.nodeValue?.trim()==='Calendar'){x.nodeValue=x.nodeValue.replace('Calendar','Email');break}const svg=n.querySelector('svg');if(svg)svg.outerHTML='<span style="width:16px;text-align:center">✉</span>';n.onclick=e=>{e.preventDefault();e.stopPropagation();openEmail()};cal.insertAdjacentElement('afterend',n)}
  let emailTries=0;const emailTimer=setInterval(()=>{captureEmail();if(V.emailTarget||emailTries++>80)clearInterval(emailTimer)},250);

  const baseOverview=typeof overviewPage==='function'?overviewPage:null,baseTasks=typeof tasksPage==='function'?tasksPage:null,baseCalendar=typeof calendarPage==='function'?calendarPage:null,baseFinance=typeof financePage==='function'?financePage:null,baseHealth=typeof healthPage==='function'?healthPage:null,baseLife=typeof lifePage==='function'?lifePage:null;
  if(baseOverview)overviewPage=overviewPageV2;if(baseTasks)tasksPage=tasksPageV2;if(baseCalendar)calendarPage=calendarPageV2;if(baseFinance)financePage=financePageV2;if(baseHealth)healthPage=healthPageV2;if(baseLife)lifePage=lifePageV2;
  const oldBind=bind;bind=function(){oldBind();try{bindV2()}catch(e){console.warn('v2 bind',e)}};
  const oldRefresh=refreshFromSupabase;refreshFromSupabase=async function(){await oldRefresh();try{await loadV2()}catch(e){console.warn('v2 data refresh',e)}render()};
  loadV2().then(()=>render()).catch(e=>console.warn('Personal OS v2 load',e));
}catch(e){console.error('Personal OS v2 disabled safely',e)}
})();
